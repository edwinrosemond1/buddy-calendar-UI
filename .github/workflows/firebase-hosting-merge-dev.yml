# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on develop
"on":
  push:
    branches:
      - develop
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: Dev
    steps:
      - uses: actions/checkout@v3
      - name: Debug Env Variables
        run: |
          echo "NODE_ENV=$NODE_ENV"
          echo "REACT_APP_BASE_URL=$REACT_APP_BASE_URL"
      - name: Install Dependencies
        run: npm install
      - name: Build
        run: npm run build
        env: # Set the environment variable for this step
          REACT_APP_BASE_URL: ${{ vars.BASE_URL }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_CALENDAR_BUDDY_BFDF1 }}"
          channelId: live
          projectId: calendar-buddy-bfdf1
